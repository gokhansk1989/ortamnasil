# Prisma bağlantısı
DATABASE_URL="postgresql://user:pass@localhost:5432/ortamnasil?schema=public"

# E-posta hash'i için pepper (gizli tut, rotate etme). Bkz. prisma/schema.prisma
AUTH_PEPPER="change-me-a-long-random-secret"

# Admin panel şifresi
ADMIN_TOKEN="degistir-beni-gizli-anahtar"
